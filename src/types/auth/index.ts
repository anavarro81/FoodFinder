type logInCredentials = {
    email: string;
    password: string;
}

type signUpCredentials = {
    name: string;
    email: string;
    password: string
}

export type {
    logInCredentials, 
    signUpCredentials
}